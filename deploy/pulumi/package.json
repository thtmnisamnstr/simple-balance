{
  "name": "simple-balance-pulumi",
  "version": "0.1.6",
  "description": "Pulumi programs that stand up the split Simple Balance deployment on EKS and on GKE.",
  "license": "AGPL-3.0-only",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p aws/tsconfig.json && tsc --noEmit -p gcp/tsconfig.json"
  },
  "dependencies": {
    "@pulumi/aws": "7.47.0",
    "@pulumi/eks": "4.3.0",
    "@pulumi/gcp": "9.37.0",
    "@pulumi/kubernetes": "4.34.2",
    "@pulumi/pulumi": "3.263.0"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "typescript": "5.9.3"
  }
}
